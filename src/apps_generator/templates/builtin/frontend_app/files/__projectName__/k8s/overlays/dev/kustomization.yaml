apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: {{ k8sNamespace }}

patches:
  - target:
      kind: Deployment
      name: {{ projectName }}
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
