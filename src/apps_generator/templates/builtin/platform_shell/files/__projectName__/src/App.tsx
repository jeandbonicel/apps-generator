// App component is no longer needed — routing is handled by router.tsx
// and rendered via <RouterProvider> in main.tsx.
// This file is kept for compatibility but the shell entry point is main.tsx.

export default function App() {
  return null;
}
