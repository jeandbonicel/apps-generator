apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ projectName }}
  namespace: {{ k8sNamespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ projectName }}
  template:
    metadata:
      labels:
        app: {{ projectName }}
    spec:
      containers:
        - name: {{ projectName }}
          image: {{ containerRegistry }}/{{ containerRegistryOrg }}/{{ projectName }}:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
