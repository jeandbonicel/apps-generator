spring:
{% if features.database %}
  datasource:
    url: jdbc:postgresql://localhost:5432/{{ dbName if dbName else projectName }}
    username: postgres
    password: postgres
  jpa:
    show-sql: true
{% endif %}
logging:
  level:
    {{ basePackage }}: DEBUG
