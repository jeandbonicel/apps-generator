apiVersion: v1
kind: Service
metadata:
  name: {{ projectName }}
  namespace: {{ k8sNamespace }}
spec:
  selector:
    app: {{ projectName }}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
